# Alternating
Use `alternating` prop to color alternating (even and odd) rows.

## Example
```vue
<template>
  <EasyDataTable
    :headers="headers"
    :items="items"
    alternating
  />
</template>
```

<Alternating/>

